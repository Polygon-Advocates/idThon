{
  "name": "solution-3",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "url": "https://github.com/wefa-tech/idThon/tree/Oba-One_idThon/solution-3"
  },
  "scripts": {
    "dev": "concurrently -n issuer,contracts,api,client -c cyan,green,yellow,magenta 'pnpm dev:issuer' 'pnpm dev:contracts' 'pnpm dev:api' 'pnpm dev:client'",
    "dev:api": "pnpm --filter 'api' run dev",
    "dev:client": "pnpm --filter 'client' run dev",
    "dev:issuer": "cd issuer && make up && make run-ui-arm",
    "dev:contracts": "cd contracts && npx hardhat run --network localhost scripts/deploy-verifier.ts"
  }
}
